"""Сценарий добавления растения через ИИ-агента.

Всегда идёт через явное подтверждение группы — ИИ только предполагает,
финальное слово за пользователем:

  1) (если есть дубль по имени в любой группе) "уже есть — добавить ещё раз?"
  2) "Добавить «X» в группу «Y»?" [Добавить] [Другая группа]
                                   [Отменить]
  3) "Другая группа" -> список всех групп пользователя + "Назад"
     (после выбора — снова экран №2, но уже с новой группой)

Ни на каком шаге новая группа автоматически не создаётся — только выбор
между уже существующими группами или "Без группы" (кроме случая, когда
пользователь сам явно назвал ещё не существующую группу — тогда она
предлагается к созданию прямо на экране №2 и создаётся по факту "Добавить",
см. group_selection.show_confirm_group / handlers.ai_confirm_add).

Публичный интерфейс не изменился при разбиении на подмодули:
- handle_add_intent — точка входа из entrypoint.handle_free_text
- match_group — сопоставление группы, предложенной ИИ, с уже существующими

Внутреннее устройство разложено по файлам:
- group_selection.py — match_group, show_confirm_group, show_pick_group
- actions.py — perform_add, success_message
- handlers.py — handle_add_intent и все @router.* хендлеры (регистрируются
  на общий router как побочный эффект импорта этого модуля)
"""

from .group_selection import match_group
from .handlers import handle_add_intent

__all__ = ["handle_add_intent", "match_group"]
