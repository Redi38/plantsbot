"""Сценарий добавления растения (FSM AddPlant).

Снаружи пакет используется так же, как раньше использовался модуль
add_flow.py — регистрация хендлеров на общий router не меняется:
    from . import add_flow  # bot/handlers/plants/__init__.py

Внутреннее устройство разложено по файлам:
- ui.py — AddPlant (FSM-состояния) и отображение шагов диалога
- actions.py — проверка повтора и финальное сохранение растения
- handlers.py — все @router.* хендлеры (регистрируются на общий router
  как побочный эффект импорта этого модуля)
"""

from . import handlers  # noqa: F401
